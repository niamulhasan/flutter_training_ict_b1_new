import 'dart:io';

void main() {
  // print("Enter a number: ");
  // int number = int.parse(stdin.readLineSync()!);
  // print(number == 0 ? "Zero" : number > 0 ? "Positive" : "Negative");

  String fruit = "banana";
  if (fruit == "mango" && fruit == "banana") {
    print("eating");
  } else {
    print("Not gonna eat");
  }
}
